<div class="card">
  <div class="card-header">
    <h1>Editing Category</h1>
  </div>
  <%= render 'form', category: @category %>
</div>

<%= link_to 'Show', settings_category_path(@category) %> |
<%= link_to 'Back', :back %>
