<div class="card">
  <div class="card-header">
    <h1>Editing Tag</h1>
  </div>
  <%= render 'form', tag: @tag %>
</div>

<div class="card">
  <%= link_to 'Show', settings_tags_path(@tag), class: "btn btn-primary" %>
  <%= link_to 'Back', settings_tags_path, class: "btn btn-info"  %>
</div>


